---
"@ai-hero/sandcastle": patch
---

Add devcontainer with Node 22, all four agent CLIs (Claude Code, Pi, Codex, OpenCode), Docker-in-Docker, Podman, GitHub CLI, GitLab CLI, and zsh (oh-my-zsh + autosuggestions, syntax-highlighting, completions). Vercel and Daytona sandbox providers work via their SDKs and `VERCEL_TOKEN` / `DAYTONA_API_KEY` env vars — no CLI needed.
